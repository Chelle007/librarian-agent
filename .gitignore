# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Local index (derived, rebuildable — not the source of truth)
*.sqlite
*.sqlite3
*.db

# The vault is a SEPARATE git repo — it holds personal data and must never be
# committed to the code repo. Seed a fresh one with: python -m librarian.cli init
/vault/

# OS
.DS_Store
