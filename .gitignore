# Secrets
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Local index (derived, rebuildable — not the source of truth)
*.sqlite
*.sqlite3
*.db

# The vault is a SEPARATE git repo living OUTSIDE this one (by default a sibling
# folder). This entry is just a safety net against an accidental nested vault/.
# Seed a fresh vault with: python -m librarian.cli init
/vault/

# Notion migration inputs — personal note exports/raw captures; keep local only
scripts/notion_export/University/
scripts/notion_pages/*.raw

# OS
.DS_Store
